1. sh tmcc_server_auto_rpm.sh 生成服务端rpm安装包
2. sh tmcc_client_auto_rpm.sh 生成客户端rpm安装包

tips:
   make sure the rpm of kernel-devel has been installed in the linux.

feaure:
   if you want to support broadcast, support_broadcast.patch is needed.
   1. cd multicast_proxy; patch -p1 < multicast_kernel/patch/support_broadcast.patch

Start on linux boot:
   chkconfig multis on --level 2345
   chkconfig multis off --level 016

   chkconfig multic on --level 2345
   chkconfig multic off --level 016
